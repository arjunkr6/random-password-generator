# Random Password Generator

A Python application that generates secure random passwords with customizable length, input validation, and a mix of uppercase, lowercase, numeric, and special characters.

## Features
- Minimum password length of 10 characters
- Guarantees at least one uppercase, lowercase, digit, and special character
- Input validation with error handling
- Simple command-line interface

## How to Run
```bash
python password_generator.py